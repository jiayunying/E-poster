<Page x:Class="E_Poster.PaperList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:E_Poster"
      mc:Ignorable="d" 
      d:DesignHeight="1920" d:DesignWidth="1080"
      Title="PaperList" Loaded="Page_Loaded">
    <Page.Resources>
        <Style TargetType="ListBox" x:Key="style_listbox">
            <Setter  Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Grid>

                            <Border>
                                <ScrollViewer BorderBrush="Transparent">
                                    <!--<UniformGrid IsItemsHost="True" Columns="1"></UniformGrid>-->
                                    <StackPanel IsItemsHost="True"/>
                                </ScrollViewer>
                            </Border>

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="AliceBlue"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" CornerRadius="10" Margin="5" >
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="true">
                                            <Setter Property="Background" TargetName="border" Value="Blue"/>
                                        </Trigger>

                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter Property="Background" TargetName="border" Value="White"/>
                                            <Setter Property="Foreground" Value="Red"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="listBoxTemplate">
            <StackPanel Margin="4">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontWeight="Bold" Text="城市名称：" DockPanel.Dock="Left" Margin="5,0,10,0"/>
                        <TextBlock Text=" " Grid.Column="1"/>
                        <TextBlock Grid.Column="2" Text="{Binding ProvinceName}" Foreground="Green" FontWeight="Bold"/>
                    </Grid>

                <DockPanel >
                    <TextBlock FontWeight="Bold" Text="创建日期：" DockPanel.Dock="Left" Margin="5,0,5,0"/>
                    <TextBlock Text=" "/>
                    <TextBlock Text="{Binding DateCreated}" Foreground="DarkOrange"/>
                    <TextBlock Text=" "/>
                    <TextBlock Text=" "/>
                    <TextBlock Text=" "/>
                    <TextBlock FontWeight="Bold" Text="测试项目：" DockPanel.Dock="Left" />
                    <TextBlock Text=" "/>
                    <TextBlock Text="{Binding DataTest}" Foreground="DarkOrange"/>
                </DockPanel>
                <DockPanel>
                    <TextBlock FontWeight="Bold" Text="更新日期：" DockPanel.Dock="Left" Margin="5,0,5,0"/>
                    <TextBlock Text=" "/>
                    <TextBlock Text="{Binding DateUpdated}" Foreground="Cyan"/>
                </DockPanel>
            </StackPanel>
        </DataTemplate>

    </Page.Resources>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="342*"/>
            <RowDefinition Height="38*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button  HorizontalAlignment="Left" VerticalAlignment="Bottom" Click="Button_Click_1" Grid.Row="0"  Width="54" Height="54" Visibility="Visible" Panel.ZIndex="10">
            <Button.Template>
                <ControlTemplate>
                    <Rectangle Margin="2">
                        <Rectangle.Fill>
                            <ImageBrush ImageSource="pack://application:,,,/resources/typelist.png"/>
                        </Rectangle.Fill>
                    </Rectangle>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Grid  HorizontalAlignment="Stretch"  Name="Thumb1" VerticalAlignment="Stretch" Grid.Row="0" Grid.RowSpan="3"  Grid.ColumnSpan="2"  Panel.ZIndex="100">
            <!--CanVerticallyScroll="True"-->
            <Grid.RenderTransform>
                <TranslateTransform x:Name="spt1"></TranslateTransform>
            </Grid.RenderTransform>
            <Grid.Clip>
                <RectangleGeometry x:Name="spc1"></RectangleGeometry>
            </Grid.Clip>
            <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            <StackPanel Background="#2B323A" Grid.Column="0">
                <StackPanel.Effect>
                    <DropShadowEffect x:Name="shadow" Color="#404851" Direction="0" ShadowDepth="10" Opacity="0.5" />
                </StackPanel.Effect>

                
                <StackPanel Margin="40,150,40,40" >
                    <Border BorderBrush="#0DBCCD" BorderThickness="1,1,1,1">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>
                            <TextBox  Grid.Column="0" x:Name="txt_search" Text="搜索"  Padding="10" Background="Transparent"  BorderThickness="0,0,0,0" Foreground="#999999" GotFocus="Search_GotFocus" LostFocus="Search_LostFocus" />
                            <Image Source="pack://application:,,,/resources/search.png" Width="30" Height="30" Grid.Column="1"></Image>
                        </Grid>
                    </Border>
                </StackPanel>
               

                <TextBlock Text="论文分类" Foreground="White" Margin="60,20" FontWeight="Bold" FontSize="20"/>
                <ListBox Name="typeList" Grid.Row="1"  DisplayMemberPath="TypeName" SelectedValuePath="ID"  VerticalAlignment="Stretch" Background="Transparent" Foreground="White" BorderThickness="0,0,0,0" GotFocus="Button_Click_1">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}" Padding="60,20">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="true">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#09B9CB" Offset="1"/>
                                                <GradientStop Color="#73EDF9" Offset="0"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <!--<Trigger Property="IsMouseOver" Value="true">
                                    <Setter Property="Background" Value="LightGreen"/>
                                    <Setter Property="Foreground" Value="Red"/>
                                </Trigger>-->
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
                <!--<Button Content="返回" Click="Button_Click_1" HorizontalAlignment="Center"></Button>-->
            </StackPanel>
            <StackPanel Background="#404851" Opacity="0.2" Grid.Column="1" Visibility="Hidden" Name="mengban" GotFocus="Button_Click_1">
            </StackPanel>
        </Grid>




        <Image x:Name="banner" Grid.Row="0" Grid.ColumnSpan="2" Stretch="Fill"/>


        <ListBox Grid.Row="1" Name="listBox1" Grid.ColumnSpan="2"  ItemsSource="{Binding Tables[0]}" 
                 ItemTemplate="{StaticResource listBoxTemplate}" Background="Transparent" SelectionChanged="ListBox1_SelectionChanged" BorderThickness="0,0,0,0"/>
        <!--翻页按钮 --> 
        <Button x:Name="Left"   HorizontalAlignment="Left"  Grid.Row="1" Grid.ColumnSpan="2" Width="95" Height="95" Visibility="Visible" Margin="12,0,0,0">
            <Button.Template>
                <ControlTemplate>
                    <Rectangle Margin="2">
                        <Rectangle.Fill>
                            <ImageBrush x:Name="Last_Imgbrush" ImageSource="pack://application:,,,/resources/left.png"/>
                        </Rectangle.Fill>
                    </Rectangle>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button x:Name="Right"   HorizontalAlignment="Right"  Grid.Row="1" Grid.ColumnSpan="2" Width="95" Height="95" Visibility="Visible"  Margin="0,0,12,0">
            <Button.Template>
                <ControlTemplate>
                    <Rectangle Margin="2">
                        <Rectangle.Fill>
                            <ImageBrush x:Name="Next_Imgbrush" ImageSource="pack://application:,,,/resources/right.png"/>
                        </Rectangle.Fill>
                    </Rectangle>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Image x:Name="bottom" Grid.Row="2" Grid.ColumnSpan="2" Stretch="None"/>
       
    </Grid>
</Page>
